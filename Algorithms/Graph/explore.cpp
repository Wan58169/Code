#include<iostream>
#include<queue>
using namespace std;
//???????????????????
//0??????? 1?????? -1????????? 
//???:????????????????
int map[100][100];
int M,N; //M????????,N????????
typedef struct {
	int x;
	int y;
}POINT; 
queue<POINT> q; //????BFS????? 
POINT s;	//??????? 

int main()
{	
	freopen("explore.txt","r",stdin);
	//????????,?????????? 
	cin >> M >> N >> s.x >> s.y;
	//????????? 
	for(int i=1; i<=N; i++) {
		for(int j=1; j<=M; j++) {
			cin >> map[i][j];
		}
	}
	map[s.x][s.y] = -1;	//?????? 
	//??????????????? 
	q.push(s);
	POINT t,newCell;
	while(q.empty() == false) {
		t = q.front();
		q.pop();
		for(int i=t.x-1; i<=t.x+1; i++) {
			for(int j=t.y-1; j<=t.y+1; j++) {
				if(i<=M&&i>=1 && j<=N&&j>=1 ) {
					//????????????????? 
					if( (i==t.x-1&&j==t.y) || (i==t.x+1&&j==t.y) ||
						(i==t.x&&j==t.y-1) || (i==t.x&&j==t.y+1)) {	
						if(map[i][j]!=0 && map[i][j]!=-1) {
							newCell.x = i;
							newCell.y = j;
							map[newCell.x][newCell.y] = -1;
							q.push(newCell);
						}
					}
				}
			}
		}
	}
	//??????
	map[s.x][s.y] = 2;
	for(int i=1; i<=M; i++) {
		for(int j=1; j<=N; j++) {
			printf("%2d ",map[i][j]);
		}
		cout << " " << endl;
	}
	return 0;
}
